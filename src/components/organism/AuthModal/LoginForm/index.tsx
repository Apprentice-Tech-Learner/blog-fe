import React from "react";

type TLoginForm = {
    onClose: () => void,
}

const LoginForm = ({ onClose }: TLoginForm) => {
    return (
        <div>
        </div>
    )
}

export default LoginForm;
